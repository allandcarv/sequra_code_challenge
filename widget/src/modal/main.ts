import { createElement } from '../shared/utils/create-element';
import mainList from './main-list';

const modalMain = createElement('main');
modalMain.appendChild(mainList);

export default modalMain;
